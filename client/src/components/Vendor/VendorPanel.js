import React from "react";
const VendorPanel = () => (
  <div>
    <h2>Vendor Dashboard</h2>
    {/* Add actions for vendor: Add course, manage enrollments */}
  </div>
);
export default VendorPanel;
