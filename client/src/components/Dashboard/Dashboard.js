import React from "react";

const Dashboard = () => (
  <div className="dashboard">
    {/* Welcome panel, cards, schedule, reminders go here */}
    <h2>Welcome back, [Student Name]!</h2>
    {/* Add grid or flex components as in attached UI */}
  </div>
);

export default Dashboard;
